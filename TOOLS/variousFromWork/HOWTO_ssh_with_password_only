When attempting to connect to a new host, If many keys are present in .ssh folder, every of it will be tried to establish the connection.

use -v option to verbose ssh output

and connect with a command like below to avoid key usage:
ssh -o PreferredAuthentications=password -v pi@comet.local

