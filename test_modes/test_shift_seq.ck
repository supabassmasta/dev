int a[39];
2 => int max_note;

fun void shift(int val){
		
		val => int remainder;
		0 => int i;

		while (remainder) {
				remainder % max_note => a[i];
				remainder / max_note => remainder;
				i++;
		}

}

shift( 126);

for (0 => int i; i <  a.size()     ; i++) {
	<<<a[i]>>>;
}
 
1::ms => now;

